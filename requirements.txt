# --- Framework & Server ---
fastapi
python-dotenv
python-multipart

# --- Database (PostgreSQL Cloud) ---
# We use psycopg[binary] for speed, but keep it minimal
sqlalchemy
psycopg2-binary
psycopg[binary]
psycopg_pool
pydantic_settings

# --- AI & Logic (Core Libraries Only) ---
# Using specific sub-libraries prevents pulling in 100MB of unused code
langchain-core
langchain-google-genai
langgraph

# --- Vector Database (Cloud Client) ---
# REPLACE ChromaDB with Pinecone or Supabase. 
# ChromaDB includes heavy local binaries (hnswlib) that bloat your bundle.
pinecone-client

# --- Security & Auth ---
python-jose[cryptography]
passlib[bcrypt]